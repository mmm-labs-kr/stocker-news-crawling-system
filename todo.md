# Theme Refinement Task

WICS 세분류 151개를 개인투자자가 직관적으로 이해 가능한 분류명으로 변환하는 작업.

---

## 결정사항 (변경 시 사용자 합의 필요)

1. **매핑 정책**: 세분류 행 151개 그대로 유지 (1행 = 1세분류). `개선된_분류` 컬럼 값은 중복 허용 → 의미 같은 세분류끼리 자연스럽게 그룹핑됨.
   - 예: "석유와 가스 시추", "석유와 가스 탐사", "석유와 가스 정제" 등 7개 행 → 모두 `개선된_분류 = "석유·가스"` 부여 (CSV는 여전히 7행 유지)
2. **임베딩 (B안)**: 기존 `세분류명` + `판별기준_포함및제외조건` 유지하고, `개선된_분류` + `개선된_설명`을 추가해서 합쳐서 임베딩.
3. **출력 파일**: `pipeline/data/themes_refined.csv` (별도 파일, 원본은 보존)
4. **컬럼 순서**: `세분류코드, 세분류명, 판별기준_포함및제외조건, 개선된_분류, 개선된_설명`
5. **백필**: 기존 `news.themes`는 백필하지 않음 (개발 단계라 신규부터 적용)
6. **모델**: Sonnet 4.6 사용. Claude Code 세션 내에서 직접 처리 (별도 LLM API 호출 스크립트 불필요).

---

## 친근한 이름(`개선된_분류`) 생성 기준

1. **타깃 독자**: 경제 뉴스를 자주 읽는 개인투자자
2. **길이**: 가급적 8자 이내. 의미 보존이 더 중요하면 초과 허용
3. **표현 규칙**:
   - 영문 병기 제거: `(Commodity Chemicals)` 같은 영문 삭제
   - WICS식 수식어("다각화된", "범용", "특수", "기타", "종합")는 본질을 가리지 않으면 제거
   - 구분자로 가운뎃점 `·` 사용 가능 (예: "석유·가스", "반도체·장비")
   - 뉴스 헤드라인에서 흔히 보이는 단어 우선

4. **그룹핑 판단 기준**:
   - **묶기 OK**: WICS가 사업 단계나 부수 활동까지 세분화한 경우 (시추/장비/탐사/정제/수송이 모두 "석유·가스")
   - **분리 유지**: 투자 관점에서 구분이 의미 있는 경우
     - 예: "금" vs "알루미늄" vs "철강" → 모두 별도 유지 (가격·수요 동인이 완전 다름)
     - 예: "범용 화학" vs "특수 화학" → 본질 같으므로 "화학소재"로 통합 가능
   - **판단 질문**: "같은 라벨로 묶으면 뉴스 분류 시 정보가 손실되거나 사용자가 혼란스러운가?" → YES면 분리

5. **`개선된_설명`**:
   - 1~2문장, 평어체
   - 개인투자자가 봤을 때 어떤 산업/사업을 다루는지 즉시 파악 가능하게
   - 판별기준의 핵심만 풀어쓰기 (제외 조건은 생략 가능)

---

## 작업 단계

### 1단계: themes_refined.csv 생성 (Claude 세션 내 직접 작업)

- [x] 1-A. 배치 1 (행 1~40) 생성 → 사용자 검수 → 필요 시 기준 보정
- [x] 1-B. 배치 2 (행 41~80) 생성 → 사용자 검수
- [x] 1-C. 배치 3 (행 81~120) 생성 → 사용자 검수
- [x] 1-D. 배치 4 (행 121~151) 생성 → 사용자 검수
- [ ] 1-E. 전체 일관성 점검:
  - 동일 의미인데 다른 라벨 쓴 항목 없는지 (예: "반도체·장비"와 "반도체장비")
  - 라벨 중복도가 적정한지 (너무 잘게 쪼개짐 / 너무 뭉뚱그려짐)
  - `개선된_분류` 고유값 개수 보고 (목표: 80~110개 정도)

### 2단계: init_vector_db.py 수정

- [ ] 2-A. CSVLoader가 `themes_refined.csv`를 사용하도록 경로 변경 (`config.py`의 `THEMES_CSV_PATH`도 함께 검토)
- [ ] 2-B. 임베딩 콘텐츠 포맷을 다음으로 변경:
  ```
  [개선된 분류] {개선된_분류}
  [설명] {개선된_설명}
  [WICS 세분류명] {세분류명}
  [판별기준] {판별기준_포함및제외조건}
  ```
  - `CSVLoader`의 기본 동작은 "컬럼명: 값" 형식이라 별도 처리 필요. 커스텀 로더 또는 row를 직접 읽어서 `Document` 객체로 변환하는 방식 고려.
- [ ] 2-C. `wics_documents` 재적재:
  - 사용자에게 TRUNCATE 실행 confirm 받기
  - 재적재 후 row 수 확인 (151행 기대)
- [ ] 2-D. 검증: 뉴스 5건으로 retriever 호출 → 상위 10개 후보가 직관적인지 sanity check

### 3단계: analyzer.py 프롬프트 수정

- [ ] 3-A. 출력 테마명을 반드시 `개선된_분류` 값으로 출력하도록 프롬프트에 명시
- [ ] 3-B. context 포맷에서 `개선된 분류` 라벨이 잘 보이도록 정렬
- [ ] 3-C. 뉴스 1~2건으로 dry-run → 출력된 테마가 친근한 이름인지 확인
- [ ] 3-D. (선택) 동일 `개선된_분류` 값이 여러 번 후보로 떠도 LLM이 중복 없이 1번만 반환하는지 확인

---

## 진행 상태

(작업 시작 후 각 단계 완료 시점에 업데이트. 사용자 검수 결과 / 발견된 이슈 / 결정 변경 사항 기록.)

- 2026-05-18: todo.md 작성. 1단계 시작 대기 중.
- 2026-05-18: themes_refined.csv 생성 완료 (151행). 사용자 최종 검수 대기 중.

---

## 참고 경로

- 원본 CSV: [pipeline/data/themes.csv](pipeline/data/themes.csv) (151행)
- 신규 CSV: `pipeline/data/themes_refined.csv` (생성 예정)
- 임베딩 스크립트: [pipeline/processors/init_vector_db.py](pipeline/processors/init_vector_db.py)
- 분석 스크립트: [pipeline/processors/analyzer.py](pipeline/processors/analyzer.py)
- DB 스키마: [supabase/migrations/002_enable_pgvector.sql](supabase/migrations/002_enable_pgvector.sql)
- 경로 설정: [pipeline/config.py](pipeline/config.py)

---

## 새 세션 진입 시 체크리스트

다른 세션에서 이 작업을 이어받을 경우:

1. 이 파일(todo.md)을 먼저 읽어서 결정사항과 기준을 확인
2. `pipeline/data/themes_refined.csv` 존재 여부와 진행 상태 확인
3. "진행 상태" 섹션에서 마지막 작업 지점 파악
4. 미완료 체크박스부터 이어서 진행
5. 작업 진행 시마다 체크박스 갱신 + 진행 상태에 메모 추가
